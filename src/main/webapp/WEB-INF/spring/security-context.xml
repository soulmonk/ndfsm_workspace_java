<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <http use-expressions="true">
        <intercept-url pattern='/*' access='permitAll'/>
        <form-login login-page="/" authentication-failure-url="/authentication-failure"
                    default-target-url="/"/>
        <logout logout-success-url="/"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service
                    data-source-ref="dataSource"
                    users-by-username-query="select login,password, enabled from users where login=?"
                    authorities-by-username-query="select u.login, r.authority from users u, user_role ur, roles r where u.id = ur.user_id and r.id = ur.role_id and u.login =?  "/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>